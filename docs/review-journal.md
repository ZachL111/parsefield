# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its parsers focus without claiming live deployment or external usage.

## Cases

- `baseline`: `token drift`, score 178, lane `ship`
- `stress`: `grammar width`, score 161, lane `ship`
- `edge`: `label quality`, score 159, lane `ship`
- `recovery`: `error locality`, score 214, lane `ship`
- `stale`: `token drift`, score 189, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
